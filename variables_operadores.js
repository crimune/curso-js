// NÚMEROS

let variableNumero = 10 // Valor numérico
let variableDecimal = 10.10 // Valor flotante

// TEXTO

let variableTexto = 'Texto'   // Valor String (cadena de caracteres)
let variableTexto2 = "Texto"  // Valor String (cadena de caracteres)
let variableTexto3 = `Esta es una variable distinta ${variableNumero}` // Expresión de JS

// OPEREDORES

const variable = 'Valor de la variable' // Operador de asignación

const suma = 1 + 3 // + es el operador aritmético de la suma
const resta = 1 - 3 // - es el operador aritmético de la resta
const multiplicacion = 1 * 3 // * es el operador aritmético de la multiplicación
const division = 1 / 3 // / es el operador aritmético de la división
const resto = 5 % 2 // % es el operador aritmético que nos dará el resto de una división
const exponente = 3 ** 3 // ** es el operadpr aritmético de la exponenciación

let x = 5;
let y = 5;

x++; // ++ para aumentar en 1 una variable
y--; // -- para disminuir en 1 una variable

console.log(y)